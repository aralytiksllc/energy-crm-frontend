// External
import { createStyles } from 'antd-style';

// Internal

export const useStyles = createStyles(() => {
  return {
    input: {
      width: '100%',
    },

    inputNumber: {
      width: '100%',
    },

    datePicker: {
      width: '100%',
    },

    select: {
      width: '100%',
    },

    textArea: {
      width: '100%',
    },
  };
});
