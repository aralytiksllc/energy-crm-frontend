import { createStyles } from 'antd-style';

export const useStatsCardsStyles = createStyles(() => ({
  cardContainer: {
    textAlign: 'center',
  },
  title: {
    margin: 0,
  },
  titleWithColor: {
    margin: 0,
  },
}));
