import { createStyles } from 'antd-style';

export const useDashboardHeaderStyles = createStyles(() => ({
  headerContainer: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  selectStyle: {
    width: 120,
  },
}));
