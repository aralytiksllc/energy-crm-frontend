import { createStyles } from 'antd-style';

export const useProductivityCardStyles = createStyles(() => ({
  container: {
    textAlign: 'center',
    padding: '20px 0',
  },
  title: {
    margin: 0,
    color: 'var(--color-success)',
  },
}));
