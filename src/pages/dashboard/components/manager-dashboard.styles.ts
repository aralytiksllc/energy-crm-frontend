import { createStyles } from 'antd-style';

export const useManagerDashboardStyles = createStyles(() => ({
  productivityReportContainer: {
    padding: '20px',
  },
  productivityCard: {
    padding: '20px',
    backgroundColor: '#ffffff',
    borderRadius: '8px',
  },
  productivityTitle: {
    fontSize: '16px',
    marginBottom: '16px',
    display: 'block',
  },
}));
