import { createStyles } from 'antd-style';

export const useViewSwitcherStyles = createStyles(({ css }) => ({
  container: css`
    padding: 350px 16px 16px 16px;
    display: flex;
    justify-content: center;
  `,
  button: css`
    width: 100%;
  `,
}));
